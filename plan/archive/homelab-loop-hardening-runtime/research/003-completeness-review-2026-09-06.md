# Independent completeness review — plan `homelab-loop-hardening-runtime`

- Repository: `/data/projects/livespec-runtime`, master at `b286237` (release 0.23.0), clean, in sync with origin.
- Ledger anchor: `livespec-runtime-mqsxsu` (epic, status `backlog`, 8/8 children closed, 33 comments).
- Reviewer: fresh, independent, read-only. Reviewed 2026-09-06.
- Inputs read in full: `research/001-charge-and-baseline-scope.md`, `research/002-contracts-drift-false-green.md`, all 33 epic comments (handoffs 1–29, the 2026-08-25 scope event, the 2026-08-28 program sync, the 2026-08-29 trigger and its correction), and every child record with its comments (`mqsxsu.1`–`.4`, `nhs`, `cr4`, `emu`, `wfl`).
- Forge verification: bulk `gh api` over closed PRs of `thewoolleyman/livespec-runtime` (#600+) and `thewoolleyman/livespec-dev-tooling` (#1633–#1646); every cited merge SHA confirmed an ancestor of local `master` with `git merge-base --is-ancestor`; `SPECIFICATION/history/v012`–`v019` confirmed present with full snapshots.
- Side effect disclosed: running `uv run pytest` dirtied `uv.lock` (the known `livespec-runtime-jis` behaviour). I restored it with `git checkout -- uv.lock`; the tree was verified clean afterwards. No ledger, git history, or project file was mutated.

## VERDICT: INCOMPLETE

One blocking finding (F-01). Everything else the plan claims to have delivered checks out against the forge and the working tree; the blocker is a deferral that lost its live home when its carrier child closed, not a false closure claim.

**Attestation of complete requirement-carrier coverage: NO.** Every carrier in the research notes and the scope event is mapped below, but three are neither discharged nor explicitly deferred to a live home (F-01 blocking; F-03 and F-04 non-blocking narrowings that should be recorded).

Remedy for the blocker is one filing: a runtime work-item (or a named child of the open `runtime-backlog-drain` epic `livespec-runtime-c7toen`) carrying the prepared payload in `plan/runtime-backlog-drain/research/005-mqsxsu4-public-surface-reconciliation.md` §5, so the 16 unratified modules and the `github_process` gap have an owner after archive.

---

## A. Requirement carriers → discharging child → verified forge evidence

Carriers are taken from research/001 (the ten-point charge plus its "verified baseline" facts), research/002 (two findings), and the 2026-08-25 scope event (five carriers). "Verified" means I checked it myself, not that the ledger says so.

| # | Requirement carrier | Source | Discharged by | Forge evidence (verified) | Working-tree check |
|---|---|---|---|---|---|
| A1 | Charge point 1: baseline ratification of the shipped attention surface (schema, kinds, ID grammar, composer semantics, validation posture) | 001 §charge 1; scope event | Spec lane (no child) | PR #607 propose, merged `cbe7c8b` 2026-08-25T11:43Z; PR #608 ratify **v012**, merged `970eea1` 2026-08-25T12:21Z; `SPECIFICATION/history/v012/` present (all 5 files + `proposed_changes/`) | `contracts.md` §`livespec_runtime.attention_item` / `needs_attention` present (HandoffKind, AttentionItem, table at lines 475–489) |
| A1a | Correct `spec.md` §"Public surface" (stale `cross_repo`-only wording) | 001 baseline fact; charge 1 | v012 | as A1 | `spec.md:117–130` now names every ratified family and "shared-runtime families"; verified |
| A1b | Scenarios + heading coverage co-edited | charge 1 | v012 + cr4/wfl/emu | as A1 | four attention scenarios in `tests/heading-coverage.json` each name a real consumer-tier test (see C) |
| A1c | State explicitly whether `hygiene_scan*`, `credentials`, `github_budget*`, `spec_governance` ride the baseline or are debt | 001 baseline fact; charge 1 | v012 proposal 6 | as A1 | `spec.md:139–147` "ACKNOWLEDGED DEBT … Each MUST be ratified or explicitly declared internal in a subsequent proposal"; verified. The *subsequent proposal* is the subject of F-01 |
| A2 | Charge point 3 (lockstep half): kind↔prefix lockstep ratified with a mechanical check; `-dnu` fixed; `human-valve`/`valve` mismatch resolved; every emitted id preserved | 001 charge 3; scope event carrier 2 | `livespec-runtime-wfl` (closed) | PR #609 merged `6421fcc` (fix: internal prefix; **TDD-Red/Green trailers verified on the commit**); PR #611 propose `6f14dbb`; PR #612 ratify **v013** `a141b90`; `history/v013/` present; `livespec-runtime-dnu` closed citing 6421fcc | `contracts.md:475–489` declared mapping table (7 kinds incl. `human-valve→valve`, `internal→internal`); `tests/consumer/test_attention_lockstep.py` exists and passes; scenario entry names `test_declared_mapping_is_total_over_ratified_kinds`; no "non-conformant" text remains in `constraints.md` |
| A3 | Charge point 3 (extension half): minimal generic-API decision, `impl:<fact-class>:<subject>` form vs new kinds, `HandoffKind` for detector-staleness decided explicitly, tests for simultaneous-facts-per-subject / deterministic ids / cross-class distinctness | 001 charge 3; homelab research/010 R4 row | **none** | — | `HandoffKind` is still exactly `drive, livespec-op, plan, shell`; no ratified text on extension posture; no such tests. See **F-03** |
| A4 | Charge point 4: retire silent drop; close unvalidated construction; negative control (one invalid + one valid ⇒ visible failure) | 001 charge 4; scope event carrier 3 | `livespec-runtime-emu` (closed) | PR #613/#614 ratify **v014** `720eec5`; PR #615/#616 ratify **v015** `6b9f73d`; PR #617 merged `f329b17` (**TDD-Red/Green trailers verified**); PR #619 `4dba3c0` Release-As; PR #618 `a2a79a7` release 0.22.0; tag `v0.22.0` confirmed on remote; `history/v014`, `v015` present | `constraints.md:77,86` clauses present; `tests/consumer/test_attention_composition_refusal.py` exists, passes; scenario entry names `test_composition_refuses_a_workflow_carrying_an_invalid_candidate` |
| A5 | Integration-tier coverage for the ratified stable-ID grammar | scope event carrier 4 | `livespec-runtime-cr4` (closed) | PR #620 merged `34dfc86` | `tests/consumer/test_attention_id_grammar.py` exists, passes; both grammar scenarios name its tests |
| A6 | Own the 29 pre-existing unowned heading-coverage TODO entries | scope event carrier 5 | `livespec-runtime-nhs` (closed) → successor `mqsxsu.2` (closed) | PR #621 `ab4b1af`; PR #623 `08847b6`; PR #624 `dad30a0`; PR #626 `aa59c69` | See C2: 0 unowned, 0 boilerplate, armed gate exit 0. Residual: 22 entries name the now-closed `mqsxsu.2` (**F-05**, non-blocking) |
| A7 | research/002 finding 1: `retry_with_backoff` ratified as `T \| None` (silent false-green) | 002 | `livespec-runtime-mqsxsu.3` (closed) | PR #625 ratify **v016** `86d5b0f`; `history/v016/` present | `contracts.md:122–128` now `-> IOResult[T, RetryExhausted]`, failure track named; verified |
| A8 | research/002 finding 2: `RefStatus` ratified as str-Enum with `RefStatus(s)` (raises TypeError) | 002 | `mqsxsu.3` | as A7 | `contracts.md:20–28` now frozen/slotted/kw-only dataclass, `RefStatus(value=s)`; positional form documented as raising; verified |
| A8a | 002 reviewer obligation: confirm no OTHER contracts.md clause still describes the pre-Railway idiom | 002 §payload | `mqsxsu.3` (3 review passes; forced `parse_depends_on_entry` correction) | as A7 | My own sweep: no `T \| None`, `str-valued Enum`, or positional `RefStatus(s)` idiom survives except the corrected mention at line 27; `parse_depends_on_entry` at 54/71 documented as returning `Failure` |
| A9 | Successor: bind the ratified version classification to the computed release bump (root of the v0.21.4 and 0.21.5 incidents) | handoff 10 (filed) | `livespec-runtime-mqsxsu.1` (closed) | dev-tooling PRs verified merged: #1633 v050 `c3044754`, #1634 v051 `5f9f2a6b`, #1635 module `c973d8e7`, #1636 release 1.33.0, #1638 v052 `c1ff9fa9`, #1641 impl `b54f5ddc`, #1642 release 1.34.0. Runtime PR #627 `1d382cc`, #628 `dc52c48` | `.github/scripts/check-pre-push.sh:19–20` invokes `livespec_dev_tooling.workflow_checks.release_bump_classification`; pin now v1.44.0 (moved on since). Note: handoff 21 cites #1638 as `0517e2da`; the forge merge SHA is `c1ff9fa9` (rebase-merge rewrite, content equivalent per handoff 21's own note) |
| A10 | Successor: reconcile the ratified inventory with the 70 exported-but-undocumented names (acceptance: every `__all__` name documented or removed; group-3 removals version-classified; explicit ride-or-debt decision per family; mechanical check) | handoff 15 (filed); scope-event deferral of module ratification pointed here | `livespec-runtime-mqsxsu.4` (closed 2026-09-06, "Fabro dispatch landed PR #660") | PR #660 merged `ad2fbd9` 2026-09-06T08:55Z; files: `mint.py`, `test_mint.py`, `test_public_surface_inventory.py`, `public-surface-debt.json`, `heading-coverage.json`, research/005, `uv.lock`; `bab3f6f feat(github_auth)!` narrowing → release **v0.23.0** (tag on remote) | Mechanical check and group-3 classification: **delivered**. Criterion 1 and the per-family ratify-or-declare decision: **not delivered** — 69 distinct names in 16 modules remain exported and undocumented, held in a shrink-only debt register; documentation half is a prepared, unfiled payload. See **F-01 (blocking)** |
| A11 | Charge point 7: repo-native evidence (Red-Green-Replay per product `.py`, Result-railway API, property-based tests on pure modules, heading-coverage co-edits, `cross_repo_public_api` reconciliation with named consumption sites, release only via release-please tag) | 001 charge 7 | partially, by process | RGR trailers verified on `6421fcc` and `f329b17`; releases v0.22.0/v0.23.0 via release-please PRs #618 and the 0.23.0 release commit | No property-based tests on `attention_item`/`needs_attention` (only `test_credentials.py` uses hypothesis); no `cross_repo_public_api` artifact exists anywhere in this repo. Neither listed as carrier nor deferred. See **F-04** |
| A12 | Charge point 10: prior art cited (`-bvtkzm`, `-xtri75`, `-o96`, `-76j`, `-dnu`, `-ego`, v008/v011 precedents); tags from authoritative sources only | 001 charge 10 | explicitly DEFERRED (scope event) | — | `-dnu` cited and closed; the rest deferred with points 2/5/6/8/9 — see B |
| A13 | Homelab debt: completion report with the ACTUAL tag to session steady-state-loop-hardening | handoffs 7–11 | handoff 11 | v0.22.0 tag verified | Homelab's 2026-08-28 sync on this epic confirms receipt; nothing owed |

Landed under this thread but not plan carriers (verified merged, listed for completeness): v017 PR #633 `d78a3b0` (closed `livespec-runtime-0h8`), v018 PR #636 `b9dabba`, v019 PR #638 `74c66d8`, #632/#637 reviewer designation, #622 `ec5ee3c` research/002, #634/#635 proposals; dev-tooling v053/#1644, #1645, #1646 (E1 commission, tracked as `livespec-dev-tooling-opdc`, now CLOSED under `kcoslm` batch 1).

## B. Explicit deferrals and their reconsideration homes

| # | What was deferred | Why (as recorded) | Where it was to be reconsidered | Does that home still exist and is it open? |
|---|---|---|---|---|
| B1 | Charge points 2 (ownership table), 5 (composer-vs-schema-only), 6 (dependency-direction proof), 9 (activation/rollback contract), 10 (prior-art pass) | "none is required to make v012 coherent" (scope event 2026-08-25T13:50Z) | "subsequent passes on this plan"; handoff 11: "reopening any of them is a fresh scope decision, not a resumption" | **Only cross-repo.** No runtime work-item or plan carries them (tenant has 6 non-closed items; none matches). The homelab program plan `steady-state-loop-hardening` is unarchived on `mi-homelab/homelab` main as of 2026-09-06T07:18Z and its board `research/013` records them as deferred; I could not read the homelab ledger (access denied) to confirm epic `hl-nkuzaz` is open. Under the 2026-08-29 ruling ("archive is terminal"), "subsequent passes on this plan" ceases to be a home at archive. See **F-02** |
| B2 | Charge point 8: release + ordered per-consumer fan-out matrix (must account for v0.21.4 as an unsequenced tag) | same scope event; handoff 5 accept-and-continue | same as B1, plus the **named reconsideration trigger** (2026-08-29T02:52Z, corrected 09:02Z): when point 8 lands, console re-files its released-producer-payload compatibility test in a NEW console plan citing `livespec-console-beads-fabro-sc23vv` | The trigger names the *consumer's* consequence and "whoever executes charge point 8", but no item names the executor. Latest tags are now v0.22.1–v0.22.3 and **v0.23.0** (2026-09-06), so the matrix's unsequenced-tag set has grown beyond v0.21.4/v0.22.0. See **F-02**, **F-07** |
| B3 | Ratifying `hygiene_scan*`, `credentials`, `github_budget*`, `spec_governance` | "v012 records them as acknowledged debt explicitly, and each needs its own symbol-level pass" | `mqsxsu.4` ("this item is where that question comes due", per its description) | `mqsxsu.4` is CLOSED. It resolved the question by pointing at the v012 debt paragraph and enumerating the 16 modules in `tests/public-surface-debt.json`; the symbol-level ratify-or-declare-internal pass is a **prepared payload** (research/005 §5, `/livespec:propose-change public-surface-inventory-completes-shipped-modules`) with **no work-item**. `livespec_runtime.cross_repo.providers.github_process` is additionally registered as `gap-not-yet-recorded-as-debt`. **No live home → F-01 (blocking)** |
| B4 | No release / consumer pin-vendor fan-out in this pass | deferred to the point 8 pass | B2 | As B2. Releases have since been cut regardless (v0.21.4 accidental, v0.22.0 planned, v0.22.1–v0.23.0 by later work); fan-out remains unsequenced |
| B5 | Handoff 21 findings A–E (dev-tooling): A doctor walks gitignored dirs ("MINE TO FILE"); B doctor static not in `just check`; C `no_stale_revise_branches` flags the revise branch; D dev-tooling `uv.lock` self-version skew; E worktree-pack absent in fresh worktrees | "logged, not filed; homelab has them in its catalogue" | dev-tooling ledger | A and C: **not found** in the dev-tooling ledger (227 items searched for gitignored/mutants/no_stale_revise_branches). E: covered by open dev-tooling worktree-pack items (`b7dbne`, `os6wd3`, …). D: runtime analogue `livespec-runtime-jis` is open. B: not found. See **F-06** (non-blocking, out-of-repo) |
| B6 | E1 N+1 predecessor-removal PR (handoffs 23–29) | blocked on orchestrator six-fact citation | `livespec-dev-tooling-opdc` | CLOSED 2026-09-06 "landed-or-falsified (kcoslm batch 1)". Home resolved; not a carrier of this plan |
| B7 | `livespec-dev-tooling-0z11` (heading_coverage verifies link existence, not assertion) | filed from v019 work | dev-tooling ledger | OPEN (P1, backlog). Live |

## C. Spot-checks against the working tree (master `b286237`)

| # | Closure | What I checked | Result |
|---|---|---|---|
| C1 | `mqsxsu.4` / PR #660 (`ad2fbd9`) | `tests/livespec_runtime/test_public_surface_inventory.py` and `tests/public-surface-debt.json` exist; ran the test file | Both exist (dated 2026-09-06). 6 tests pass. The debt file registers **16 unratified modules** — `credentials`; `github_budget`, `_client`, `_client_support`, `_measurement`, `_types`; `hygiene_scan`, `_cli`, `_context`, `_findings`, `_types`, `_worktree_dirt`, `_worktree_merge`, `_worktrees`; `spec_governance` (all "acknowledged-debt") — plus `cross_repo.providers.github_process` as **"gap-not-yet-recorded-as-debt"** (`completed_gh` undocumented), and the `# @generated` port `_fractional_indexing` with 5 names taken out of scope. Group 2 is empty: `GithubFailure` documented at `contracts.md:91`; `GithubQueryFailed`/`GithubBudgetUnmeasurable` are still only *named* in the union at line 92, not defined as their own bullets (the queue head `mqsxsu.3` flagged; incompleteness, not contradiction) |
| C2 | `nhs` / `mqsxsu.2` | Parsed `tests/heading-coverage.json`; ran `LIVESPEC_FAIL_IF_HEADING_COVERAGE_TODOS_EXIST=true just check-no-todo-registry` and `just check-heading-coverage` | 47 entries; 22 `TODO`; **0 unowned**; **0** `li-univck` boilerplate reasons; every TODO reason is heading-specific. Both gates exit **0**. All 22 TODO owners are `livespec-runtime-mqsxsu.2` (closed); the gate logs `work-item liveness UNVERIFIED (no reachable tracker configured)` for each — see F-05 |
| C3 | `mqsxsu.3` / v016 | Read `contracts.md` retry, RefStatus, parse_depends_on_entry clauses; swept for stale idioms | Retry: `Callable[[], IOResult[T, E]] -> IOResult[T, RetryExhausted]`, IOFailure on exhaustion. RefStatus: dataclass, `RefStatus(value=s)`, positional raises. `parse_depends_on_entry` returns `Failure`. No pre-Railway idiom remains. Reads as corrected |
| C4 | `wfl` / v013 | `contracts.md` declared mapping table; `tests/consumer/test_attention_lockstep.py`; scenario entry | Table lists 7 kinds with `human-valve→valve` and `internal→internal`; test passes; scenario names `test_declared_mapping_is_total_over_ratified_kinds`. `livespec-runtime-dnu` closed citing 6421fcc |
| C5 | `emu` / v014–v015, PR #617 | consumer refusal test; unit tests; TDD trailers on `f329b17` | Tests pass (34 across the six attention/inventory files); trailers present (Red captured 16:01:58Z, Green 16:04:02Z) |
| C6 | `cr4` / PR #620 | `tests/consumer/test_attention_id_grammar.py`; scenario entries | Exists; both scenarios map to its tests; passes |
| C7 | `mqsxsu.1` / PR #627 | `.github/scripts/check-pre-push.sh` | Lines 19–20 invoke the release-bump classification check; banner text matches the close reason |

## D. Findings

**F-01 (BLOCKING) — The module-ratification deferral lost its home when `mqsxsu.4` closed.** The scope event deferred ratifying `hygiene_scan*`, `credentials`, `github_budget*`, `spec_governance` because "each needs its own symbol-level pass", and `mqsxsu.4` declared itself "where that question comes due". `mqsxsu.4`'s own acceptance criterion 1 ("every name … either appears in contracts.md … or has been removed from `__all__`") is not met: 69 distinct names across 16 modules are still exported and undocumented, now held in a shrink-only register. The maintainer's 2026-09-06 06:42 comment split the item so the documentation half needed explicit authorization; the dispatch did not receive it and correctly stopped at a prepared payload (`plan/runtime-backlog-drain/research/005 §5`), and then the item was closed on "Fabro dispatch landed PR #660" without a home for that half. The `runtime-backlog-drain` epic `c7toen` has zero children. Additionally `cross_repo.providers.github_process` is a fresh, unrecorded gap (`completed_gh` undocumented; not covered by the spec.md debt paragraph). Remedy: one work-item carrying research/005 §5 (github_process disposition + per-family ratify-or-declare-internal proposal) before archive, and cite it in the archive record.

**F-02 (non-blocking, must be recorded at archive) — Charge points 2/5/6/8/9/10 have no runtime-side home after archive.** They were deferred "to subsequent passes on this plan"; archive is terminal by the 2026-08-29 ruling. The only live home is cross-repo: homelab's program plan `steady-state-loop-hardening` (unarchived on remote main 2026-09-06) and its board. I could not verify epic `hl-nkuzaz` in the homelab ledger (access denied). For point 8 the console trigger names the consumer consequence but not an executor. The archive record should name the home explicitly (homelab program epic/board, or a new runtime plan pattern) rather than "subsequent passes on this plan".

**F-03 (non-blocking) — Charge point 3's extension half was narrowed silently.** research/001 and homelab research/010 R4 require a "minimal generic-API decision", an explicit `HandoffKind` decision for detector-staleness handoffs, and tests for simultaneous-facts-per-subject / deterministic ids / cross-class distinctness. The scope event carried only the lockstep half; nothing records "no extension needed". In practice no producer demanded one (handoff 26: the orchestrator's needs-attention items were dependency-free backlog) and homelab's 2026-08-28 sync treats point 3 as done, so the charging party accepted the narrowing. One sentence in the archive record recording that disposition closes this.

**F-04 (non-blocking) — Charge point 7 is neither a carrier nor a deferral.** Red-Green-Replay pairs (verified on both product commits), heading-coverage co-edits, and release-please-only releases were honoured. Property-based tests on the pure attention modules were not added, and no `cross_repo_public_api` reconciliation artifact exists in this repository. Record as consciously exempted or fold into F-01's item.

**F-05 (non-blocking) — 22 heading-coverage TODO entries name the closed `mqsxsu.2`.** This is exactly the liveness trap `nhs` re-pointed 29 entries to avoid; it passes today only because liveness checking is `UNVERIFIED`. The `mqsxsu.4` comment "parked" this from `mkn`, and `mqsxsu.4` is now closed too. The entries are terminal prose/check-enforced dispositions with specific reasons, so the fix is cosmetic (re-home the `work_item` field or the gate's closed-owner rule), but it needs an owner.

**F-06 (non-blocking, out-of-repo) — Two thread-owed dev-tooling findings were never filed.** Handoff 21 finding A (doctor static walks gitignored `mutants/`) was declared "MINE TO FILE"; finding C (`no_stale_revise_branches` flags the very branch a revise runs on, making the override structurally required). Neither appears in the dev-tooling ledger. Finding B (doctor static is not part of `just check`) also not found.

**F-07 (informational) — The tag record in the timeline is stale.** Handoffs through 29 state the latest tag is v0.22.0; the remote now carries v0.22.1, v0.22.2, v0.22.3 and v0.23.0 (cut 2026-09-06 by `mqsxsu.4`'s `feat(github_auth)!` narrowing, correctly classified Major → minor under the pre-major provision). Point 8's eventual matrix must sequence all of them. Also cosmetic: the epic's `next_action` metadata is truncated ("…is actionable. Work"), and handoff 21 cites dev-tooling #1638 as `0517e2da` where the forge merge SHA is `c1ff9fa9`.

## E. Verdict

**INCOMPLETE.** Blocking: F-01. Non-blocking findings to record in the archive evidence: F-02, F-03, F-04, F-05, F-06, F-07.

Attestation of complete requirement-carrier coverage: **NO** (F-01, F-03, F-04). Every cited PR is merged on `github.com/thewoolleyman/livespec-runtime` and every cited `SPECIFICATION/history/vNNN` (v012–v019) exists; all seven spot-checks pass on their delivered halves. Once F-01 has a filed, open home (a single work-item), and F-02/F-03/F-04 are recorded as dispositions on the epic, this review would read COMPLETE.

---

## F. Re-verification (2026-09-06, after remedies)

Reviewer identity: `completeness-reviewer subagent, session 5f41fc26, 2026-09-06`. Read-only; nothing mutated. Each claim below was read from the ledger or forge directly, not taken from the requesting message.

| Finding | Claimed remedy | What I verified | Result |
|---|---|---|---|
| F-01 (blocking) | New work-item `livespec-runtime-x87` carries research/005 §5 | `bd show livespec-runtime-x87 --json`: exists, status `backlog` (a valid open `WorkItemStatus`), type task, P2, created 2026-09-06T09:10:09Z, one dependency edge `discovered-from: livespec-runtime-mqsxsu.4`. Description names the runtime-backlog-drain epic `c7toen` as its discovering plan, cites research/005 §5 by path, and scopes (1) the `github_process` gap disposition, (2) the per-family ratify-or-declare-internal propose-change `public-surface-inventory-completes-shipped-modules` for all 16 modules by name, (3) shrinking `public-surface-debt.json` to zero, (4) F-05 re-homing of the 22 closed-owner heading-coverage entries and F-04's property-based-test / `cross_repo_public_api` exemption. Acceptance criteria (a)–(d) are concrete and mechanically checkable (register empty, a ratified vNNN, no closed-owner entry, `just check` green). Dispatch split recorded (payload prep dispatchable; spec lane maintainer-gated). | **Discharged.** The deferral now has a named, open home; F-04 and F-05 are folded in explicitly |
| F-02 | Dispositions comment names the homelab program plan / `hl-nkuzaz` as the live home for charge points 2/5/6/8/9/10 | Epic comment dated 2026-09-06 09:10 ("ARCHIVE DISPOSITIONS") read from `bd comments livespec-runtime-mqsxsu` (comment count 33 → 34). It replaces "subsequent passes on this plan" with the homelab plan `steady-state-loop-hardening`, epic `hl-nkuzaz`, board research/013, names point 8's executor as "whichever session homelab charges", and keeps the console trigger (`sc23vv`) as the consumer-side consequence. On `mi-homelab/homelab` main at `d9e1359a` (2026-09-06T07:18Z) the plan directory is still under `plan/`, not `plan/archive/`, and its `associated_work_item_id` file reads `hl-nkuzaz`. The homelab ledger itself remains unreadable from this host (access denied), so the epic's open status is inferred from the unarchived plan directory | **Explicitly deferred to a live home** (with the ledger-access caveat) |
| F-03 | Point 3's extension half disposed as not-needed | Same comment: disposed with the reasoning (no producer demanded an extension per handoff 26; homelab's 2026-08-28 sync accepted point 3; `HandoffKind` stays the four ratified members; reopening is a fresh scope decision on the homelab program plan). Consistent with what I found in the working tree | **Recorded disposition** |
| F-04 | Folded into x87 | x87 scope item 4 names it | **Transferred** |
| F-05 | Folded into x87 | x87 scope item 4 and acceptance (c) name it | **Transferred** |
| F-06 | A filed as `livespec-njkg` (core tenant); B and C parked on `livespec-dev-tooling-kcoslm` | `bd show livespec-njkg` in `/data/projects/livespec`: exists, BUG, P3, backlog, created 2026-09-06, describes the gitignored `mutants/` doctor-static defect (re-filed from `livespec-runtime-f5zhs5`). `bd show livespec-dev-tooling-kcoslm`: epic, backlog, plan `dev-tooling-backlog-drain`; its 2026-09-06 09:10 "PARKING LOT" comment names (B) doctor-static not in `just check` and (C) `no_stale_revise_branches` flags the revise branch, and points A at `livespec-njkg` | **Live homes confirmed** |
| F-07 | Tag set recorded; `next_action` superseded by archive | Same comment records v0.22.1–v0.22.3 and v0.23.0 for point 8's matrix. The epic's `next_action` metadata is still the truncated string (updated_at unchanged, 07:09Z); the comment states the archive itself supersedes it | **Recorded**; cosmetic residue only |

Archive rule applied: remaining work is transferred to named follow-up work-items (`livespec-runtime-x87`; cross-tenant `livespec-njkg`, `livespec-dev-tooling-kcoslm`; cross-repo `hl-nkuzaz`), and the disposition comment states those names exactly. The archive record itself, when written, must repeat `livespec-runtime-x87` verbatim as the transfer target; the disposition comment already does.

### Updated VERDICT: COMPLETE

Every requirement carrier in research/001, research/002 and the 2026-08-25 scope event is now either discharged (A1–A2, A4–A9, A11-process elements, A13) or explicitly deferred to a named live home (A3 → disposed on record; A10/B3 → `livespec-runtime-x87`; B1/B2/B4 → homelab `steady-state-loop-hardening` / `hl-nkuzaz`; B5 → `livespec-njkg` and `livespec-dev-tooling-kcoslm`). All spot-checks pass.

Attestation of complete requirement-carrier coverage: **YES**.

Remaining blocking findings: **none**. Residual caveats, non-blocking: the homelab ledger could not be read directly (home inferred from the unarchived plan directory and its `associated_work_item_id`); the epic `next_action` metadata is stale until archive overwrites it.
