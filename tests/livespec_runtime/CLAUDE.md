# tests/livespec_runtime/

Mirrors `livespec_runtime/` one-to-one. Test files at this level
cover modules directly under `livespec_runtime/` (top-level smoke
tests, etc.); subdirectories mirror the package's subpackages
(`cross_repo/`).
